- name: Install
  package:
    name: samba
    state: present
  register: samba_install
  notify: restart_samba
  tags:
    - install

- name: 'Create config'
  template:
    src: smb.conf.j2
    dest: '/etc/samba/smb.conf'
    mode: '0700'
  register: samba_config
  notify: reload_samba
  tags:
    - config
